<style>
  .profile-section {
    display: flex;
    align-items: center;
  }
  .profile-image {
    width: 100px;
    height: 100px;
    border-radius: 50%;
    display: block;
  }
  .profile-text {
    padding-right: 20px; /* Add some space between the image and the text */
  }
</style>
<header class="profile-section">
  <div class="profile-text">
    <a href="/"><h2>{{ title }}</h2></a>
    <p>{{ body }}</p>
  </div>
  <img src="{{ image_url }}" alt="Trevor Clarke" class="profile-image" />
</header>
